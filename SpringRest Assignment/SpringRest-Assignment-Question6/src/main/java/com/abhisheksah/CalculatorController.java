package com.abhisheksah;

import org.springframework.web.bind.annotation.RestController;


@RestController
public class CalculatorController {
	
	
	
/*   
    private final Long value;
    
    public Calculator() {
        value = 0L;
    }
    
    public Calculator(Long initialValue) {
        this.value = initialValue;
    }

    @GET
    public Long getValue() {
        return value;
    }

    @Path("+{value}")
    public Calculator add(@PathParam("value") Long value) {
        return new Calculator(this.value + value);
    }
    
    @Path("-{value}")
    public Calculator subtract(@PathParam("value") Long value) {
        return new Calculator(this.value - value);
    }
    
    @Path("*{value}")
    public Calculator multiply(@PathParam("value") Long value) {
        return new Calculator(this.value * value);
    }
*/
	
}
